#CAVE - Man muss mit R Studio die Arbeitsumgebung über die .rproj Datei öffnen! Sonst stimmt
#das Verzeichnis nicht mehr

# Auswertung der ersten Umfrage
# Globale Dinge, wie das Laden bestimmter Bibliotheken, werden in der globals.R gemacht, auf die dann referenziert wird.

#normalerweise wird das in der globals.R geladen, aber ich setze das Verzeichnis hier erneut, damit ich nicht mit der
#globals.R starten muss

source("globals.R") #Grundeinstellungen laden
source("Datenbereinigung.R")





